[package]
name = "aco-mission-control"
version = "0.1.0"
description = "ACO Mission Control - HACO adaptive 2-opt benchmark simulator"
authors = ["Joshua Bermas"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
parking_lot = "0.12"

# pytauri: embed the Python algorithm engine
pyo3 = { version = "0.23", features = ["auto-initialize"] }

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
